# ============================================
# 连锁门店视频管理系统 — 门店客户端环境变量配置
# ============================================
#
# 后端 API 地址。
# - 浏览器开发模式（npm run dev）：vite proxy 自动转发 /api → localhost:3000，无需设置此项。
# - Electron 模式（npm run electron:dev / 打包版）：渲染进程走 file:// 协议，vite proxy
#   不生效，必须设置此项，否则所有 API 请求会失败（表现为登录页二维码加载失败）。
#
# 复制本文件为 .env 后按实际情况修改：
#   copy .env.example .env
# ============================================
VITE_API_BASE_URL=http://localhost:3000/api/v1
